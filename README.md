# Mercury

Mercury is a minimal multi-agent orchestrator focused on:

- parse-first boundaries
- async DAG execution
- rule-first planning with optional reasoning escalation
- state checkpoints and replay

Run tests:

```bash
UV_CACHE_DIR=.uv-cache .venv/bin/uv run --python .venv/bin/python -m pytest -q
```
